# The assistant tab's chat endpoint. Same origin, proxied by nginx to the RAG
# service on :8001, so there is no CORS surface and no second hostname to hold
# a certificate. Empty or absent, the tab says "Not connected" rather than
# posting questions into the dark.
VITE_ASSISTANT_URL=/assistant/chat
