<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <!-- The device token and the unuploaded position buffer never leave the
         handset. Backup and device-transfer are refused outright rather than
         filtered: a restored copy of this app on a second phone would report a
         second position for the same bus. Re-pair instead. -->
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
