<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns everything above this. The platform theme only has to keep
         the window from flashing the wrong colour before setContent runs. -->
    <style name="Theme.SmsGateway" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@color/window_background</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
