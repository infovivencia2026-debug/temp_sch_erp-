<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <!-- The device token and the message queue never leave the handset. Backup
         and device-transfer are both refused outright rather than filtered,
         because there is nothing here worth restoring: re-pair instead. -->
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
